# API — Initial Design

Base path: `/api/v1`

- `GET /health` — service health
- `/auth` — authentication endpoints
- `/documents` — upload and document management
- `/processing` — processing jobs and status
- `/ai` — summaries, extraction and document Q&A
- `/exports` — CSV/Excel/JSON export
